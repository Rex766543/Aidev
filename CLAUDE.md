# CLAUDE.md

This file provides guidance to Claude Code when working in this repository.

## Project Overview

This is an AI development workspace. Currently it contains MCP (Model Context Protocol) configuration.

## MCP Configuration

`mcp.json` defines MCP servers available to this project:

- **playwright**: Browser automation via `@playwright/mcp@latest` — enables web browsing, scraping, and UI testing through Claude.

## Development Notes

- This is a git repository on branch `feature1`
- Add new MCP servers to `mcp.json` under `mcpServers`
