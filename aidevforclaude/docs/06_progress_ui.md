# 06 進捗バー UI（Udemy ライク）

## TODO

### 進捗データの取得
- [×] 認証済みユーザーの `user_progress` を、コース内の全動画IDを条件にクライアントサイドで取得する
- [×] 取得したデータから「完了済み動画数 / 総動画数」を計算する

### 進捗バーコンポーネント
- [×] `components/ProgressBar.tsx` を `"use client"` で作成する
- [×] パーセント表示（例: `67%`）をテキストで表示する
- [×] 色付きプログレスバー（Udemy ライク）を実装する
  - 背景色：グレー系（ダークテーマに合わせる）
  - 進捗色：インジゴ（0-99%）/ 緑（100%）
  - バーの幅を `style={{ width: \`${percent}%\` }}` で動的に設定する
- [×] 0%・100%の場合に適切な表示（「まだ開始していません」「🎉 完了！」）をする
- [×] モバイル対応（レスポンシブ）を実装する

### 講座詳細ページへの組み込み（`/courses/[id]`）
- [×] `CourseCurriculum.tsx`（Client Component）を作成し `ProgressBar` と動画リストを統合する
- [×] 未認証ユーザーには進捗バーを表示しない（代わりに CTA バナーを表示）

### 動画視聴ページのサイドバーへの組み込み（`/courses/[id]/videos/[videoId]`）
- [×] サイドバー上部にコース全体の進捗バーを表示する
- [×] 動画完了後にリアルタイムで進捗バーを更新する（`revalidatePath` による再フェッチ）

### 動画リストの完了チェックマーク
- [×] 完了済み動画のアイテムにチェックマークアイコンを表示する（CourseCurriculum・VideoPage サイドバー両方）
- [×] 現在再生中の動画を視覚的に区別する（アクティブスタイル：インジゴ左ボーダー）

### 動作確認
- [ ] 動画を完了するたびにパーセントが正しく更新されることを確認する
- [ ] 全動画完了時に進捗バーが 100% になることを確認する
- [ ] 未認証時に進捗バーが表示されないことを確認する
- [ ] モバイル表示が崩れていないことを確認する
