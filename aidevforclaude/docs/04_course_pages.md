# 04 講座一覧・詳細ページ（SSR）

## TODO

### 講座一覧ページ（`/`）
- [ ] `app/page.tsx` を実装する（Server Component・SSR）
- [ ] Supabase から `is_published = true` の講座を取得する
- [ ] 講座カード一覧をグリッドレイアウトで表示する
- [ ] 各カードに表示する情報を実装する
  - [ ] サムネイル画像（`next/image` 使用）
  - [ ] 講座タイトル
  - [ ] 説明（短文・truncate）
  - [ ] 総動画数
  - [ ] カテゴリタグ（`courses` テーブルにカテゴリカラムが必要な場合は追加する）
- [ ] ダーク系デザインでスタイリングする
- [ ] モバイル対応（レスポンシブ）を実装する
- [ ] 講座が0件の場合の空状態 UI を実装する

### 講座詳細ページ（`/courses/[id]`）
- [ ] `app/courses/[id]/page.tsx` を実装する（Server Component・SSR）
- [ ] `params.id` で講座データを取得する（存在しない場合は `notFound()` を呼ぶ）
- [ ] 講座のカリキュラム構成を表示する（セクション → 動画の階層）
- [ ] セクションごとに動画リストを表示する（`order_index` 順）
- [ ] 各動画アイテムに「完了済み」チェックマーク表示エリアを確保する（進捗はクライアントサイドで取得）
- [ ] 第1動画は未ログインでも視聴リンクを表示する
- [ ] 第2動画以降は未ログイン時にログインページへのリンクを表示する（`/login?next=/courses/[id]/videos/[videoId]`）
- [ ] 進捗バーの表示エリアを用意する（実装は `06_progress_ui.md` で行う）
- [ ] ダーク系デザイン・モバイル対応を実装する

### ローディング・エラー対応
- [ ] `app/courses/[id]/loading.tsx` を作成する（スケルトン UI）
- [ ] `app/courses/[id]/error.tsx` を作成する（`"use client"` 必須）

### 動作確認
- [ ] 未ログイン状態で講座一覧・詳細が表示できることを確認する
- [ ] 講座一覧に `is_published = false` の講座が表示されないことを確認する
- [ ] 第2動画以降のリンクが未ログイン時にログインページへリダイレクトされることを確認する
- [ ] 存在しない講座 ID へのアクセスで 404 ページが表示されることを確認する
- [ ] モバイル表示が崩れていないことを確認する
