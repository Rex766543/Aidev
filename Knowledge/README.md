# Knowledge

This folder stores only durable Knowledge explicitly recorded with `/reco`.

Do not add ordinary notes here unless the user explicitly asks to record them with `/reco`.

Start with `Knowledge/CLAUDE.md` for the operating rules.
